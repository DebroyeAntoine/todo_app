name: todos_repository_firestore
description: A repository that handles todo in firestore.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.12.7
  rxdart: ^0.22.0
  equatable: ^0.3.0
  firebase_core: ^0.4.0+7
